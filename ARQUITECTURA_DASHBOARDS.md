# 📊 ARQUITECTURA DE DASHBOARDS - CUNDIPARK

## Flujo de Autenticación y Redirección

```
┌─────────────────────────────────────────────────────────────────┐
│                    USUARIO ACCEDE AL SITIO                      │
│                   http://localhost:8080/                        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
      ┌──────────────────────────────────┐
      │   ¿Usuario Autenticado?          │
      └───┬────────────────────────┬─────┘
          │ NO                     │ SÍ
          ▼                        ▼
    ┌─────────────┐       ┌───────────────────┐
    │   /login    │       │ Obtener rol del   │
    │             │       │ usuario           │
    └─────────────┘       └───┬──────┬────┬───┘
                              │      │    │
                        SUPERADMIN ADMIN USER
                              │      │    │
                    ┌─────────┘      │    └────────────┐
                    │               │                 │
                    ▼               ▼                 ▼
            ┌──────────────┐  ┌─────────────┐  ┌──────────┐
            │ /superuser/  │  │ /admin/     │  │ /home    │
            │ dashboard    │  │ dashboard   │  │          │
            └──────────────┘  └─────────────┘  └──────────┘
```

---

## 1️⃣ DASHBOARD DE USUARIO (/home)

```
┌─────────────────────────────────────────────────────────┐
│                    DASHBOARD USUARIO (USER)             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  UserController.home()                                │
│  ├─ Obtiene: user, saludo, vehiculoActual, bloques    │
│  │                                                    │
│  └─► templates/dashboard.html                        │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  Sección 1: Estado de tu Vehículo              │  │
│  ├─────────────────────────────────────────────────┤  │
│  │ • Si tiene vehículo activo:                    │  │
│  │   ├─ Placa: ${vehiculoActual.vehiculo.placa} │ │
│  │   ├─ Ubicación: ${vehiculoActual.bloque}     │ │ 
│  │   ├─ Ingreso: ${vehiculoActual.createdAt}    │ │
│  │   └─ Botón: "Registrar Salida" → /salida     │ │
│  │                                                │ │
│  │ • Si no tiene vehículo:                       │ │
│  │   └─ Mensaje: "No tienes vehículos"          │ │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  Sección 2: Registrar Entrada (si disponible)  │  │
│  ├─────────────────────────────────────────────────┤  │
│  │ • Formulario con campos:                       │  │
│  │   ├─ Placa del vehículo (ABC123)             │  │
│  │   ├─ Marca, modelo, color                    │  │
│  │   ├─ Fecha SOAT Vencimiento                  │  │
│  │   ├─ Fecha Tecno Vencimiento                 │  │
│  │   ├─ Seleccionar bloque de bloques activos   │  │
│  │   └─ Botón enviar → /entrada (POST)          │  │
│  │                                                │  │
│  │ • Deshabilitado si ya hay vehículo activo    │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  Enlace: "Ver Historial" → /historico               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Controlador:** `UserController.java`  
**Métodos clave:**
- `home()` - Carga el dashboard
- `entrada()` - POST para registrar entrada
- `salida()` - POST para registrar salida

---

## 2️⃣ DASHBOARD DE ADMINISTRADOR (/admin/dashboard)

```
┌──────────────────────────────────────────────────────────┐
│               DASHBOARD ADMINISTRADOR (ADMIN)            │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  AdminController.dashboard()                           │
│  ├─ Obtiene: user, estadísticas, vehículos activos    │
│  │                                                     │
│  └─► templates/adminDashboard.html                   │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Tarjetas de Estadísticas (4 columnas)           │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ ┌────────────┐ ┌────────────┐ ┌────────────┐    │  │
│  │ │ Usuarios   │ │ Vehículos  │ │ Ocupación  │    │  │
│  │ │ ${total}   │ │ Activos    │ │ ${ocup}%   │    │  │
│  │ │            │ │ ${activos} │ │ [status]   │    │  │
│  │ └────────────┘ └────────────┘ └────────────┘    │  │
│  │ ┌────────────┐                                  │  │
│  │ │ Entradas   │                                  │  │
│  │ │ 24h        │                                  │  │
│  │ │ ${entradas}│                                  │  │
│  │ └────────────┘                                  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Tabla: Vehículos Actualmente en Parqueadero    │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ Placa | Usuario | Ubicación | Hora Ingreso     │  │
│  │ ─────────────────────────────────────────────  │  │
│  │ AAA123| Juan    | Bloque A  | 10:30 AM        │  │
│  │ BBB456| María   | Bloque B  | 11:00 AM        │  │
│  │       │         │          │ Acciones:        │  │
│  │       │         │          │ ├─ Cambiar bloque│  │
│  │       │         │          │ └─ Eliminar ╳    │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Tabla: Últimos Registros (últimas 24h)         │  │
│  ├──────────────────────────────────────────────────┤  │
│  │ Placa | Usuario | Bloque | Hora | SOAT | Tecno │  │
│  │ ─────────────────────────────────────────────  │  │
│  │ CCC789| Pedro   | Bloque C| 15:20| ✓    | ✓    │  │
│  │ DDD000| Laura   | Bloque D| 16:45| ✓    | ✓    │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  Botones: [Ver Gestión de Usuarios] [Backup SQL]       │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**Controlador:** `AdminController.java`  
**Datos enviados:**
- `totalUsuarios` ← `userService.getAllUsers().size()`
- `vehiculosActivos` ← `registroService.countActiveRegistros()`
- `ocupacionGeneral` ← Cálculo de porcentaje
- `entradas24h` ← `registroService.countEntradasLast24Hours()`
- `vehiculosActivosList` ← `registroService.findByActivoTrue()`
- `registros` ← `registroService.findTopNByCreatedAtDesc(10)`

---

## 3️⃣ DASHBOARD DE SUPERADMIN (/superuser/dashboard)

```
┌────────────────────────────────────────────────────────┐
│            DASHBOARD SUPERADMIN (SUPERADMIN)           │
├────────────────────────────────────────────────────────┤
│                                                        │
│  SuperUserController.dashboard()                      │
│  ├─ Obtiene: usuarios, estadísticas, búsqueda       │
│  │                                                   │
│  └─► templates/superuserDashboard.html             │
│                                                        │
│  ┌────────────────────────────────────────────────┐  │
│  │  Tarjetas de Resumen (3 columnas)               │  │
│  ├────────────────────────────────────────────────┤  │
│  │ ┌──────────────┐ ┌──────────────┐ ┌──────┐   │  │
│  │ │ Total        │ │ Usuarios     │ │ Admins│   │  │
│  │ │ Usuarios     │ │ Activos      │ │       │   │  │
│  │ │ ${total}     │ │ ${activos}   │ │${admins}  │  │
│  │ └──────────────┘ └──────────────┘ └──────┘   │  │
│  └────────────────────────────────────────────────┘  │
│                                                        │
│  ┌────────────────────────────────────────────────┐  │
│  │  Buscador de Usuarios                          │  │
│  ├────────────────────────────────────────────────┤  │
│  │ [🔍 Buscar por nombre o correo...] [Buscar]   │  │
│  │ [Limpiar]                                      │  │
│  └────────────────────────────────────────────────┘  │
│                                                        │
│  ┌────────────────────────────────────────────────┐  │
│  │  Tabla: Listado General de Usuarios            │  │
│  ├────────────────────────────────────────────────┤  │
│  │ ID | Usuario | Email | Rol | Vehículo | Act  │  │
│  │ ────────────────────────────────────────────  │  │
│  │ 1  | Juan    | j@... | USER | ABC123   | ✓   │  │
│  │ 2  | María   | m@... | ADMIN| BBB456   | ✓   │  │
│  │ 3  | Pedro   | p@... | USER | ─        | ✗   │  │
│  │    │         │       │      │ SOAT/Tecno│    │  │
│  │    │         │       │      │ ──────   │    │  │
│  │    │ ACCIONES:       │      │ ✓  ✓    │    │  │
│  │    │ ├─ Cambiar Rol  │      │          │    │  │
│  │    │ ├─ Activar/Inac │      │          │    │  │
│  │    │ └─ Eliminar ╳   │      │          │    │  │
│  └────────────────────────────────────────────────┘  │
│                                                        │
│  Enlace: [Volver al Dashboard Admin]                │
│                                                        │
└────────────────────────────────────────────────────────┘
```

**Controlador:** `SuperUserController.java`  
**Datos enviados:**
- `totalUsuarios` ← `userService.getAllUsers().size()`
- `usuariosActivos` ← `userService.countActiveUsers()`
- `totalAdmins` ← `userService.countUsersByRole(RolUsuario.ADMIN)`
- `listausuarios` ← Búsqueda filtrada o todos los usuarios

---

## 📋 Datos de Prueba para cada Dashboard

### Dashboard Usuario
```
GET /home
Datos requeridos:
- user = User{id=1, email="usuario@test.com", rol=USER}
- vehiculoActual = Registro{id=1, placa="ABC123", ...}
- bloques = List<Bloque>{...}
```

### Dashboard Admin
```
GET /admin/dashboard
Datos requeridos:
- user = User{id=2, email="admin@test.com", rol=ADMIN}
- totalUsuarios = 15
- vehiculosActivos = 8
- ocupacionGeneral = "53.3"
- vehiculosActivosList = List de Registros activos
```

### Dashboard SuperAdmin
```
GET /superuser/dashboard
Datos requeridos:
- user = User{id=3, email="superadmin@test.com", rol=SUPERADMIN}
- totalUsuarios = 15
- usuariosActivos = 12
- totalAdmins = 3
- listausuarios = List<User> filtrada o completa
```

---

## ✅ Checklist de Funcionalidad

```
DASHBOARD USUARIO:
  ✅ Mostrar vehículo activo
  ✅ Opción de registrar salida
  ✅ Formulario de entrada si no hay vehículo
  ✅ Seleccionar bloque del formulario

DASHBOARD ADMIN:
  ✅ Tarjetas de estadísticas dinámicas
  ✅ Tabla de vehículos activos
  ✅ Tabla de últimos registros
  ✅ Botones de gestión (eliminar, cambiar bloque)
  ✅ Acceso a gestión de usuarios

DASHBOARD SUPERADMIN:
  ✅ Tarjetas resumen (total, activos, admins)
  ✅ Buscador de usuarios funcional
  ✅ Tabla de usuarios con detalles
  ✅ Cambiar rol de usuarios
  ✅ Activar/desactivar usuarios
```

---

## 🎯 Endpoints Principales

```
AUTENTICACIÓN:
├─ GET  /                    → Redirige a /home o /login
├─ GET  /login               → Formulario de login
├─ POST /login               → Procesar login
└─ GET  /logout              → Cerrar sesión

USUARIO (USER):
├─ GET  /home                → Dashboard usuario
├─ POST /entrada             → Registrar entrada vehículo
├─ POST /salida/{id}         → Registrar salida vehículo
└─ GET  /historico           → Ver historial movimientos

ADMIN (ADMIN):
├─ GET  /admin/dashboard     → Dashboard admin
├─ POST /admin/delete-registro/{id} → Eliminar registro
├─ GET  /admin/backup/sql    → Descargar backup SQL
└─ GET  /superuser/dashboard → Redireccionar a SuperAdmin

SUPERADMIN (SUPERADMIN):
├─ GET  /superuser/dashboard → Dashboard superadmin
├─ GET  /superuser/dashboard?search=... → Buscar usuarios
├─ POST /superuser/changerole/{id}      → Cambiar rol usuario
├─ POST /superuser/togglestatus/{id}    → Activar/inactivar
├─ POST /superuser/deleteuser/{id}      → Eliminar usuario
└─ POST /superuser/updateuser           → Actualizar datos usuario
```

---

## 🚀 Estado Final: ✅ COMPLETAMENTE FUNCIONAL
